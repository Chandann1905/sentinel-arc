# System Architecture
Sentinel Arc is built on Domain Driven Design (DDD) principles. The `project-brain-core` crate houses pure domain models and traits, while `project-brain-knowledge` contains the implementation details and engines.
